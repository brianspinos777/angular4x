import { MySummaryPipe } from './my-summary.pipe';

describe('MySummaryPipe', () => {
  it('create an instance', () => {
    const pipe = new MySummaryPipe();
    expect(pipe).toBeTruthy();
  });
});
