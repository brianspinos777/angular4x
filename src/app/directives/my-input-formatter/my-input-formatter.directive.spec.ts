import { MyInputFormatterDirective } from './my-input-formatter.directive';

describe('MyInputFormatterDirective', () => {
  it('should create an instance', () => {
    const directive = new MyInputFormatterDirective();
    expect(directive).toBeTruthy();
  });
});
